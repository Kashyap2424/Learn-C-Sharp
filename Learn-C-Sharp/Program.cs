using System; // this all namespace store in our .NET Framework

namespace LearnCSharp // This namespace used for organize our and is collection of classes, interfaces, structs, enum and delegets.
{
    class MainClass
    {
        public static void Main(string[] args) // This is the Main method, Main method is the entry point of our console application.
        {
            Console.WriteLine("Hello C#!"); // This is Console class this store in 'using System' and here we are use they class.
        }
    }
}
